import styles from './Header.module.scss';

const Header = () => {
  return (
    <header id="main-header" className={styles.header}>
      <h1>Epic Battle of the Circles</h1>
    </header>
  );
};

export default Header;
